# --- core: the text pipeline and API. The system runs on these alone. ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
pydantic==2.10.4
python-multipart==0.0.20
numpy>=1.26

# --- supervised models (M10) and validation (M7) ---
pandas>=2.0
scikit-learn>=1.4
scipy>=1.11
joblib>=1.3

# --- multimodal tier (M12-M15, M19). Optional at runtime: these modules
#     report "not available" and the text pipeline is unaffected if absent. ---
librosa>=0.10
soundfile>=0.12
opencv-python-headless>=4.9
mediapipe==0.10.14

# --- tests ---
pytest==8.3.4
httpx==0.28.1
